export const dynamic = "force-dynamic";
import { CustomerSelector } from "@/components/dashboard/customer-selector";

export default function TestPage() {
    return <CustomerSelector onSelect={() => { }} />;
}
